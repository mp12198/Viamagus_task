import React from 'react';
import {View} from 'react-native';
import {Colors} from '../../constants/Colors';

const GamesPlayed = () => {
  return (
    <View style={{backgroundColor: Colors.offWhite, height: '100%'}}></View>
  );
};

export default GamesPlayed;
